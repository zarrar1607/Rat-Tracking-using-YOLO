tracker_type: bytetrack # tracker type, ['botsort', 'bytetrack']
track_high_thresh: 0.1 # threshold for the first association
track_low_thresh: 0.1 # threshold for the second association
new_track_thresh: 0.2 # threshold for init new track if the detection does not match any tracks
track_buffer: 150 # buffer to calculate the time when to remove tracks
match_thresh: 0.7 # threshold for matching tracks
fuse_score: True # Whether to fuse confidence scores with the iou distances before matching
# min_box_area: 70  # threshold for min box areas(for tracker evaluation, not used for now)